# Placeholder — unit tests are added progressively per phase.
# This file exists so pytest finds the tests/unit/ directory in CI.


def test_placeholder():
    """Remove this test when the first real unit test is added."""
    assert True
