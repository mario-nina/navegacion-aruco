[build-system]
requires      = ["setuptools>=68"]
build-backend = "setuptools.backends.legacy:build"

[project]
name            = "navegacion-aruco"
version         = "0.0.0"
description     = "Robot móvil autónomo con navegación por marcadores ArUco y OpenCV en Raspberry Pi 3B"
requires-python = ">=3.11"

