# Dependencias para la Raspberry Pi 3B
# Instalar con: pip install -r requirements-pi.txt
#
# Incluye runtime + herramientas mínimas de testing
# NO incluye herramientas de desarrollo (ruff, pre-commit, etc.)

-r requirements.txt

# Testing en hardware
pytest>=8.0
pytest-cov>=5.0
